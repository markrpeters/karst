[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "karst"
version = "0.1.0"
description = "Reads parsed evidence and argues a verdict. First public component: the agent-loop guards."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
authors = [{ name = "Mark Peters" }]
keywords = ["llm", "agent", "tool-calling", "ollama", "harness", "guardrails", "dfir", "forensics"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "pydantic>=2,<3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.5",
]
# The Ollama-backed Model implementation is import-guarded; install this
# extra only if you want to drive the loop against a live local model.
ollama = [
    "ollama>=0.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/karst"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "B", "UP"]
ignore = [
    "E501",  # long lines in fixture strings and guidance text
]
